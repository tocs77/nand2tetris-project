import { SubroutineCall } from '../ast/types';
import { SymbolTable } from './types';

export const generateSubroutineCall = (subroutineCall: SubroutineCall, symbolTable: SymbolTable) => {
  console.log(subroutineCall);
  const methosSymbolTable: SymbolTable = {};
  
};
